@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


    @if (SignInManager.IsSignedIn(User))
    {
       <div class="collapse navbar-collapse justify-content-end"  id="login-dropdown">
         <ul class="navbar-nav">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-dark" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Konto
                </a>
                <ul class="dropdown-menu dropdown-menu-end">                   
                    <li>
                        <button class="nav-link text-dark dropdown-item text-wrap" onclick="window.location.href='/Identity/Account/Manage/Index'" title="Manage"><span style="margin-left:15px">@User.Identity?.Name</span></button>
                    </li>
                    <li>
                        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                            <button type="submit" id="logout" class="nav-link btn btn-link text-dark dropdown-item text-wrap"><span style="margin-left:15px">Logga ut</span></button>
                        </form>
                    </li>
                </ul>
            </li>
         </ul>
       </div>

      @* <ul class="navbar-nav flex-column">
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@User.Identity?.Name</a>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" id="logout" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>
      </ul> *@

    }
    else
    {
      <div class="collapse navbar-collapse justify-content-end" id="login-dropdown">
        <ul class="navbar-nav">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Logga in
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li><button onclick="window.location.href='/Identity/Account/Login'" class="dropdown-item text-wrap">Logga in</button></li>
                    <li><button onclick="window.location.href='/Identity/Account/Register'" class="dropdown-item text-wrap">Registrera dig</button></li>
                </ul>
            </li>
        </ul>
      </div>
    }
